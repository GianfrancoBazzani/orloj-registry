# target/ is ~163MB of host build cache and is rebuilt inside the image anyway —
# sending it as build context would dominate the build time.
target/

# Regenerated by build.rs during the image build.
src/abi/uniswap_v3_pool.rs

*.spkg
.git/
README.md
setup.sh
