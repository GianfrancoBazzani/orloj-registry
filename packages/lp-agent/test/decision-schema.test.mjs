/**
 * AI decision JSON validation — implemented with T6.
 */

import { describe, it } from "node:test";

describe("decision-schema (pending T6)", () => {
  it.todo("accepts HOLD | REDUCE_LIQUIDITY and rejects CLAIM_FEES / malformed");
});
