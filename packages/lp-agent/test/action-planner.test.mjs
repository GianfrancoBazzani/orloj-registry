/**
 * Decision → proposed MCP action mapping — implemented with T7.
 */

import { describe, it } from "node:test";

describe("action-planner (pending T7)", () => {
  it.todo("maps HOLD to no write and REDUCE_LIQUIDITY to decrease_v3_position");
});
