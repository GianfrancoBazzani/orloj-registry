/**
 * Orloj MCP JSON-RPC response parsing — implemented with T3.
 */

import { describe, it } from "node:test";

describe("mcp-response (pending T3)", () => {
  it.todo("parses tools/call content and surfaces isError without logging secrets");
});
