/**
 * Deterministic market / position feature extraction.
 * Timestamp windows (not array positions); insufficient-sample flags; conditional USD.
 * Implemented in T5.
 */

/**
 * @returns {never}
 */
export function extractFeatures() {
  throw new Error("extractFeatures is not implemented yet (T5)");
}
