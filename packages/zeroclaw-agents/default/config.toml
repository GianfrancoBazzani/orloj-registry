schema_version = 3

[secrets]
encrypt = false

[runtime_profiles.unbounded]
agentic = true
agentic_timeout_secs = 1800
compact_context = false
delegation_timeout_secs = 900
keep_tool_context_turns = 8
max_actions_per_hour = 4294967295
max_context_tokens = 128000
max_cost_per_day_cents = 4294967295
max_delegation_depth = 8
max_history_messages = 200
max_system_prompt_chars = 64000
max_tool_iterations = 100
max_tool_result_chars = 64000
memory_recall_limit = 10
parallel_tools = true
shell_timeout_secs = 600
strict_tool_parsing = false

[risk_profiles.yolo]
allowed_commands = ["*"]
auto_approve = ["*"]
block_high_risk_commands = false
level = "full"
require_approval_for_medium_risk = false
sandbox_enabled = false
workspace_only = false

[providers]

[runtime_profiles.unbounded.context_compression]
enabled = true
identifier_policy = "strict"
max_passes = 3
protect_first_n = 3
protect_last_n = 4
source_max_chars = 50000
summary_max_chars = 4000
threshold_ratio = 0.5
timeout_secs = 60
tool_result_retrim_chars = 2000

[risk_profiles.yolo.delegation_policy]
mode = "allow"

[memory]
backend = "markdown.markdown"

[providers.models]

[runtime_profiles.unbounded.eval]
enabled = false
max_retries = 1
min_quality_score = 0.5

[providers.models.custom]

[runtime_profiles.unbounded.history_pruning]
collapse_tool_results = true
enabled = false
keep_recent = 4
max_tokens = 8192

[providers.models.custom.default]
model = "qwen3.7-max"
uri = "https://router-api.0g.ai/v1"
api_key = ""

[agents]

[agents.default]
runtime_profile = "unbounded"
model_provider = "custom.default"
risk_profile = "yolo"
mcp_bundles = ["remote"]
acp_enable_mcp = true

[runtime_profiles]

[runtime_profiles.unbounded.thinking]
default_level = "medium"
native_thinking = false

[runtime_profiles.unbounded.tool_receipts]
enabled = false
inject_system_prompt = true
show_in_response = false

[onboard_state]
quickstart_completed = true

[risk_profiles]

# No [mcp_bundles.remote] here. The app owns that table: writeMcpSelection emits it in the
# managed block on every session start, even for an empty selection. Defining it here too is
# a TOML duplicate key, which zeroclaw answers by resetting the whole config to defaults.
