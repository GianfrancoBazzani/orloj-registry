DATABASE_URL=""

# 1Claw vault provider. Note: packages/app uses `1CLAW_API_KEY` (leading
# digit), but bash env-file parsers reject identifiers that start with a
# digit, so the registry uses ONECLAW_API_KEY.
ONECLAW_API_KEY=""

# SpaceComputer / Orbitport KMS credentials.
ORBITPORT_CLIENT_ID=""
ORBITPORT_CLIENT_SECRET=""
